#!/usr/bin/env bash
set -euo pipefail
ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
mkdir -p "$ROOT/artifacts/models/pair34"
exec "$ROOT/scripts/train_pair34" --output-dir "$ROOT/artifacts/models/pair34" "$@"
